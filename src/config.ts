// Reserved documentation domain (IANA), used here as a clearly-inert
// placeholder until the app actually ships to a store in Phase 7. Replace
// with the real store link at that point.
export const SHARE_URL = 'https://example.com/dontmove';
